# KT-L10 验收证据

日期：2026-08-16  
环境：当期受控验收环境（历史记录）

## 风险政策与确定性评价

- 风险政策 `RISK-KT-P1-2026` V1 已发布：最低毛利率 25%、逾期宽限 0 天、信用到期提前 30 天预警。
- V2 已完成草稿和发布演练，生效日为 2027-01-01；数据库只允许草稿原值转换为已发布，发布后不可修改或删除。
- 评价由服务器读取固定订单、报价毛利、信用决定和应收余额，浏览器不能传入分数、严重度或命中结论。
- 每次评价固定政策版本、规范输入、规则轨迹、结论、建议动作、有效期、执行人、关联 ID 和 SHA-256 规范哈希。
- 规则轨迹覆盖低毛利、逾期应收、信用到期、合同签署和订单释放门禁；未命中规则也保留可解释轨迹。

## 责任任务闭环

P1 订单 `SO-KT-P1-DEMO` 毛利率 24.10%，低于 V1 的 25% 门槛，得到：

| 项目     | 结果                                     |
| -------- | ---------------------------------------- |
| 严重度   | HIGH                                     |
| 风险分数 | 45                                       |
| 命中规则 | `LOW_MARGIN`                             |
| 建议动作 | 复核利润与例外审批                       |
| 任务序列 | OPEN → ACKNOWLEDGED → ESCALATED → CLOSED |
| 关闭证据 | `RISK-KT-P1-001`，例外审批已归档         |

高风险评价在同一数据库事务内自动创建责任任务。任务只允许追加事件；序号必须连续、状态跳转受约束，关闭必须包含非空证据。历史评价和任务事件禁止更新或删除。

## 权限与产品界面

- `risk-policy:read/manage` 与 `risk:read/evaluate/manage` 分离；API 默认拒绝并继续执行客户 DataScope。
- API 测试验证 TEAM 范围传递、评价/处理授权以及仅有读取权限时拒绝写入。
- 字段化工作台提供政策创建、修订、发布、订单评价、责任人、命中原因和事件时间线，不依赖 JSON 输入。
- Order 360 按 `risk:read` 单独裁剪风险分区及事件，当前聚合 1 条评价和 18 条跨模块证据。

## 完整门禁

- 格式、ESLint、TypeScript、全仓构建：通过；
- 高危生产依赖审计：`No known vulnerabilities found`；
- 数据库迁移静态测试：18 项通过；
- Web 单元测试：18 项通过；
- API 非数据库单元测试：63 项通过；
- 204 隔离 `kingturf_test` API 全量测试（含 PostgreSQL）：102 项通过；
- 可重复演示验证器：通过；
- Playwright 真实 204 页面 E2E：6 项通过。
