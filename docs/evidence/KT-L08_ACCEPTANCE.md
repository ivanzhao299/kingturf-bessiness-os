# KT-L08 验收证据

日期：2026-08-16  
环境：204 预览环境，经本地隧道 `http://127.0.0.1:14331`

## 政策与计提口径

- 政策 `COM-KT-P1-2026` V1 已发布并固定在佣金案例上；基础佣金率 3%，最低毛利率 20%，全额回款后释放。
- 已验证同一政策可创建和发布未来生效的 V2，历史案例仍引用 V1，不随新版本漂移。
- 计提金额完全由服务器读取订单 CNY 950,000、报价毛利 CNY 229,000（24.10%）及回款台账计算，结果为 CNY 28,500。
- 规范输入、规则计算轨迹、政策版本与 SHA-256 规范哈希永久保留。

## 不可变台账场景

预览案例 `21a7abe1-d19a-4a69-803e-0393146718da` 完成以下顺序：

1. `ACCRUED`：按固定政策版本计提；
2. `FROZEN`：首款 CNY 500,000，回款比例 52.63%，未达到 100% 门槛；
3. `RELEASED`：尾款 CNY 450,000 核销后，服务端重新验证全额回款；
4. `PAID`：记录工资凭证 `PAYROLL-COM-2026-08-001`；
5. `CLAWED_BACK`：按已支付金额全额追回，凭证 `CLAWBACK-COM-2026-08-001`。

数据库触发器拒绝非法状态跳转、非连续序号、错误支付/追回金额以及对历史台账的更新或删除。提前释放实测返回 `409 conflict`。

## 可重复演示与页面

`pnpm demo:seed` 已扩展为完整佣金场景，重跑会复用固定业务编号和幂等键，不重复创建业务单据或追加终态台账。`pnpm demo:verify` 从 API 重新读取并断言：

- 订单、政策版本、受益人和会计期间引用一致；
- 计提收入、毛利、回款快照与 CNY 28,500 结果一致；
- 五条台账序号连续、状态顺序准确、每条均有 64 位规范哈希。

字段化页面提供政策创建、版本修订/发布、订单计提以及冻结、释放、支付、追回和取消操作；桌面与 390×844 移动端均可查看经济口径和完整时间线，不依赖原始 JSON 输入。

## 完整门禁

- 格式、ESLint、TypeScript、全仓构建：通过；
- 高危生产依赖审计：`No known vulnerabilities found`；
- 数据库迁移静态测试：17 项通过；
- Web 单元测试：18 项通过；
- API 非数据库单元测试：61 项通过；
- 204 隔离 `kingturf_test` API 全量测试（含 PostgreSQL）：100 项通过；
- Playwright 真实 204 页面 E2E：4 项通过。
