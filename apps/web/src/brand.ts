import mark from './assets/kingturf-mark.webp?inline';

// A 128px derivative of the original brand art: enough for the 64px UI at 2x.
// Inline it so a slow independent PNG transfer cannot leave a partially drawn mark.
export const brandMark = mark;
